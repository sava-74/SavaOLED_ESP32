# 📺 SavaOLED_ESP32 — Высокопроизводительная библиотека для OLED-дисплеев на ESP32

> 💡 **Для SSD1306, I2C, ESP32 (Arduino IDE / ESP-IDF) core 3.x.x**  
> 🔤 Поддержка UTF-8 (кириллица), скроллинг, вертикальный текст, примитивы, многошрифтовые строки  
> ⚙️ Оптимизирована под стабильную работу с `i2c_master.h`  
> 📦 Готова к установке через Arduino IDE  

---

## 🌟 Особенности

✅ **UTF-8 → CP1251** — поддержка русского языка  
✅ **Многошрифтовые строки** — до 8 сегментов с разными шрифтами  
✅ **Горизонтальный и вертикальный скроллинг** — бегущая строка и вертикальные графики  
✅ **Режимы отрисовки**: `REPLACE`, `ADD_UP` (наложение), `INV_AUTO` (XOR), `ERASE`  
✅ **Графические примитивы**: точка, линия, круг, прямоугольник, кривые Безье  
✅ **Высокая производительность** — оптимизированные алгоритмы, буферизация  
✅ **Поддержка ESP-IDF** — использует `i2c_master.h`, не зависит от `Wire`  

---

## 📥 Установка

### Через Arduino IDE

1. Скачайте архив: [SavaOLED_ESP32.zip](https://example.com/SavaOLED_ESP32.zip) *(ссылка условная)*
2. В Arduino IDE:  
   **Скетч → Подключить библиотеку → Добавить .ZIP библиотеку**
3. Перезапустите IDE
4. Примеры: **Файл → Примеры → SavaOLED ESP32**

### Вручную

1. Скачайте репозиторий
2. Поместите папку `SavaOLED_ESP32` в:
~/Arduino/libraries/

3. Перезапустите IDE

---

## 🔌 Подключение

```bash
#include <SavaOLED_ESP32.h>

// 128x64, I2C_NUM_0
SavaOLED_ESP32 oled(128, 64, I2C_NUM_0);

void setup() {
 oled.init();           // 400kHz, SDA=21, SCL=22
 oled.clearBuffer();
}
📌 По умолчанию:

Адрес I2C: 0x3C
Пины: SDA=21, SCL=22
Частота: 400 кГц

```

📚 API: Полное описание

🏁 Конструктор

### SavaOLED_ESP32(uint8_t width = 128,  uint8_t height = 64,  i2c_port_t port = I2C_NUM_0);

Создаёт объект дисплея.

| Параметр | Тип | Описание |
| :--- | :--- | :--- |
| width | uint8_t | Ширина экрана (по умолч. 128) |
| height | uint8_t | Высота экрана (по умолч. 64) |
| port | i2c_port_t | Порт I2C: I2C_NUM_0 или I2C_NUM_1 |

---

🔌 init()

### void init(uint32_t freq = 400000, int8_t _sda = 21, int8_t _scl = 22);

Инициализирует I2C и дисплей.

| Параметр | Тип | Описание | 
|---------|-----|--------| 
| freq | uint32_t | Частота I2C (Гц), по умолч. 400000 max 1200000 | 
| _sda | int8_t | Пин SDA (по умолч. 21) | 
| _scl | int8_t | Пин SCL (по умолч. 22) |

---

🖌️ setFont()

### void setFont(const Font &font);

* Устанавливает шрифт для последующих print().

| Параметр | Тип | Описание | 
|---------|-----|--------| 
| font | const Font& | Ссылка на шрифт Fonts\SavaFont_5x8.h  |

---

📏 setCharSpacing()

### void setCharSpacing(uint8_t spacing);

Задаёт промежуток между символами.

| Параметр | Тип | Описание | 
|---------|-----|--------| 
| spacing | uint8_t | Количество пикселей (по умолч. 1) |

---

🎨 setDrawMode()

### void setDrawMode(uint8_t mode = REPLACE);
Режим отрисовки пикселей.

| Параметр | Тип | Описание | 
|---------|-----|--------| 
| mode | uint8_t | REPLACE, ADD_UP, INV_AUTO, ERASE, ERASE_BORDER |

| Режим | Описание | 
|------|--------| 
| REPLACE | очистит и поверх нарисует (рекомендуется) | 
| ADD_UP | Наложение (OR), прозрачный фон | 
| INV_AUTO | Авто-Инверсия (XOR) | 
|ERASE | Стирание под собой (только примитивы) | 
| ERASE_BORDER | Стирание + белый контур (только примитивы) |
  
  ---
  
✍️ print() — вывод текста

Все print() накапливают текст. Вызовите drawPrint() для отображения.

### void print(const char* text)

| Параметр | Тип | Описание | 
|---------|-----|--------| 
| text | const char* | C-строка в UTF-8 (поддерживается кириллица) |

### void print(int value, uint8_t min_digits = 0)

| Параметр | Тип | Описание | 
|---------|-----|--------| 
| value | int | Целое число | 
| min_digits | uint8_t | Мин. длина (доп. нулями, напр. 007) |

✅ Поддержка: int8_t, int16_t, int32_t, uint8_t, uint16_t, uint32_t

### void print(float **value**, uint8_t **decimalPlaces** = 2, uint8_t **min_width** = 0)
| Параметр | Тип | Описание | 
|---------|-----|--------| 
| value | float | Дробное число | 
| decimalPlaces | uint8_t | Знаков после запятой | 
| min_width | uint8_t | Мин. ширина (доп. пробелами) |

✅ Аналогично для double

### void print(const String &s)
| Параметр | Тип | Описание | 
|---------|-----|--------| 
| s | const String& | Объект String Arduino |

---

📍 Управление курсором

#### void setCursor(int16_t x, int16_t y, uint8_t align = StrLeft, int16_t x2 = -1)

| Параметр | Тип | Описание | 
|---------|-----|--------| 
| x | int16_t | Левая X координата | 
| y | int16_t | Y координата строки | 
| align | uint8_t | Выравнивание: StrLeft, StrCenter, StrRight, StrScroll | 
| x2 | int16_t | Правая граница (если > 0) |

---

* int16_t getCursorX() const
Возвращает текущую X позицию.

---

* int16_t getCursorY() const
Возвращает текущую Y позицию.

---

* uint16_t getTextWidth() const
Ширина строки в пикселях.

---

* uint16_t getTextHeight() const
Высота строки по шрифту.

---

🖼️ Отрисовка

### void drawPrint()

Рисует накопленную строку в буфер. Учитывает выравнивание и скроллинг.

---

### void drawPrintVert()

Выводит текст вертикально (сверху вниз). Требует шрифт, повернутый на 90°.

---

### void display()

Отправляет буфер на дисплей.

---

### void clearBuffer()

Очищает буфер (всё чёрное).

---

🎨 Примитивы

### void dot(int16_t x, int16_t y, uint8_t mode = REPLACE)

Точка.

void line(...), hLine(...), vLine(...)
Линии.

void circle(...), rect(...), rectR(...)
Круг, прямоугольник, скруглённый прямоугольник.

void drawBitmap(...)
Битмап 1 бит/пиксель.

void bezier(...), drawPeak(...)
Кривые Безье и кривые, проходящие через три точки.

---

🔧 Настройки дисплея

| Функция | Назначение | 
|--------|----------| 
| setContrast(value) | Контраст (0..255) | 
|setPower(mode) | Вкл/выкл дисплея | 
| flipH(mode), flipV(mode) | Отражение экрана | 
| invertDisplay(mode) | Инверсия цветов | 
| setRotation(rotate180) | Поворот на 180° |

🌀 Скроллинг
| Функция | Назначение | 
|--------|----------| 
| setScroll(enabled) | Вкл/выкл скроллинг | 
| setScrollSpeed(speed, loop) | Скорость (1–10) | 
| setScrollSpeedVert(speed) | Вертикальная скорость (1–10)|

---

📦 Режим буферизации

### void setBuffer(bool enabled);

| Параметр | Тип | Описание | 
|---------|-----|--------| 
| enabled | bool | true — целиком, false — постранично |

✅ true — быстрее, false — стабильнее

📂 Примеры
BasicText
```bash
oled.clear();
oled.print("Привет, мир!");
oled.print(3.14);
oled.drawPrint();
oled.display();
```
```bash
oled.clear();
oled.setScrollSpeed(5, true);  // в void setup
oled.setCursor(0, 20, StrScroll);
oled.setScroll(true);
oled.setDrawMode(INV_AUTO);
oled.setCharSpacing(1);
oled.print("Бегущая строка на OLED! ");
oled.drawPrint();
oled.display();
```
* VerticalText
```bash
oled.clear();
oled.setFont(font_Vert9);
oled.setCursor(10, 0);
oled.print("Temp: ");
oled.print(25.5);
oled.drawPrintVert();
oled.display();
```
* GraphicsDemo
```bash
oled.line(0, 0, 127, 63, ADD_UP);
oled.circle(64, 32, 20, REPLACE, true);
oled.rectR(20, 20, 80, 20, 8, INV_AUTO, false);
oled.display();
```
* MixedFonts
```bash
oled.setFont(font_Small);
oled.print("Счёт: ");
oled.setFont(font_Big);
oled.print(100);
oled.setFont(font_Small);
oled.print(" очков");
oled.drawPrint();
oled.display();
```
---

# 🛠 Требования

Платформа: ESP32 ядро v3.x.x
---
Среда: Arduino IDE 
---
Библиотеки: driver/i2c_master.h (входит в ESP-IDF)
---
📄 Лицензия

MIT — свободное использование и модификация.
---
📬 Автор
👤 SavaLab
📧 sava-74@inbox.ru
🔗 [github.com/sava/SavaOLED_ESP32](https://github.com/sava-74/SavaOLED_ESP32.git)


✅ Готов к использованию!
Откройте примеры в Arduino IDE и начните творить.
